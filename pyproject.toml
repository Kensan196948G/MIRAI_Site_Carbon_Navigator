[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["frontend"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B"]
ignore = ["B008"]  # FastAPI dependency-injection defaults (Depends(...))
