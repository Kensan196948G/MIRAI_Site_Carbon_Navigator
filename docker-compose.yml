services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/carbon_navigator.db
      - MIRAI_CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
    restart: unless-stopped
